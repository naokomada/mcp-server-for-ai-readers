# .env.example — frontend 用の環境変数テンプレート
# このファイルを .env にコピーし、値を埋めてください。
# Next.js は開発/本番ともに .env を自動で読み込みます。

############################################################
# 必須（本リポジトリのサーバー処理で使用）
############################################################
# Alchemy の RPC エンドポイント（最優先で参照されます）
# 例: https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_API_KEY>
ALCHEMY_RPC_URL=

# 以下はエイリアス（ALCHEMY_RPC_URL が未設定のときにだけ参照）。
# どれか1つが設定されていれば OK です。
# ALCHEMY_HTTP_URL=
# ALCHEMY_API_URL=

############################################################
# 任意（SSE 利用時などで mcp-handler が参照する可能性のある変数）
############################################################
# SSE を有効化する場合は、Redis 互換のストア URL を指定してください。
# 現状は app/mcp/route.ts で disableSse: true になっているため未使用です。
# REDIS_URL=
# KV_URL=

# メモ:
# - NFT メタデータ取得（lib/nftTokenUrl.ts）は ALCHEMY_RPC_URL 等を使用します。
# - ALCHEMY_RPC_URL が設定されていない場合、実行時にエラーになります。
